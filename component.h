#ifndef CP#define CP#include <string>#include <set>class Tile {    public:    int numb;    int value;    int type;    bool geese;        bool hasGeese();    int getType();    int getVal();    Tile();    std::set<int> returnVertex();    // returns the vertex of the tile and put them into a std::set    std::set<int> returnEdge();    // returns the edge of the tile and put them into a std::set};class Vertex {    public:    std::string building;    int owner;    int vertex;    Vertex();    std::string returnOwner();    std::string returnBuilding();    std::set<int> returnEdges();    std::set<int> returnTiles();  };class Edge {    public:    int owner;    int edge;    std::string hasRoad();    std::set<int> returnVertex();     Edge();};#endif
